# دليل إكمال قاعدة البيانات

تم تحديث قاعدة البيانات لتتوافق مع المخطط الكامل للمنصة التعليمية.

## الملفات المتوفرة:

### 1. `database.sql` - الملف الكامل
   - يحتوي على جميع الجداول مع الحقول المحدثة
   - استخدمه لإنشاء قاعدة البيانات من الصفر

### 2. `complete_database.sql` - ملف التحديث
   - يستخدم لتحديث الجداول الموجودة
   - يضيف الحقول الجديدة للجداول الحالية
   - ينشئ الجداول الجديدة

## الجداول المحدثة:

### ✅ Content (المحتوى التعليمي)
**الحقول المضافة:**
- `title` - العنوان
- `topic` - الموضوع
- `category` - الفئة
- `difficulty` - الصعوبة (سهل، متوسط، صعب)
- `min_age` - الحد الأدنى للعمر
- `max_age` - الحد الأقصى للعمر

### ✅ Tasks (المهام)
**الحقول المضافة:**
- `status` - الحالة (pending, in_progress, completed, skipped)
- `time_completed` - الوقت المستغرق
- `parent_note` - ملاحظات الأهل

**جدول جديد: `child_tasks`**
- يربط المهام بالأطفال والوالدين
- يتضمن: child_id, task_id, parent_id, status, time_completed, parent_note

### ✅ Sessions (الجلسات)
**الحقول المضافة:**
- `parent_id` - معرف الأهل
- `content_id` - معرف المحتوى

### ✅ Badges (الشارات)
**الحقول المضافة:**
- `min_star` - الحد الأدنى للنجوم
- `max_star` - الحد الأقصى للنجوم

## الجداول الجديدة:

### 📊 Reports (التقارير)
```sql
- report_id
- parent_id
- child_id
- session_id
- achievement (الإنجازات)
- report_data (بيانات JSON)
- pages_rendered (عدد الصفحات)
- total_time (الوقت الإجمالي)
- generated_at
- viewed_by_parent
- viewed_at
```

### 🏆 child_badges (شارات الأطفال)
```sql
- child_badge_id
- child_id
- badge_id
- stars_earned (عدد النجوم)
- awarded_at
```

### ✅ task_completions (إكمال المهام)
```sql
- completion_id
- child_id
- task_id
- session_id
- parent_id
- completion_time
- duration_taken_minutes
- score (النتيجة)
```

## خطوات التنفيذ:

### إذا كانت قاعدة البيانات موجودة بالفعل:

1. افتح **phpMyAdmin**
2. اختر قاعدة البيانات `kids_learning`
3. انقر على تبويب **SQL**
4. انسخ محتوى ملف `complete_database.sql`
5. الصقه في مربع SQL
6. انقر على **تنفيذ** (Go)

### إذا كنت تريد إنشاء قاعدة البيانات من الصفر:

1. افتح **phpMyAdmin**
2. انقر على تبويب **SQL**
3. انسخ محتوى ملف `database.sql`
4. الصقه في مربع SQL
5. انقر على **تنفيذ** (Go)

## ملاحظات مهمة:

⚠️ **تحذير:** 
- إذا كانت الجداول موجودة بالفعل، استخدم `complete_database.sql` فقط
- قد تظهر أخطاء إذا كانت الحقول موجودة مسبقاً - يمكنك تجاهلها
- احتفظ بنسخة احتياطية من قاعدة البيانات قبل التحديث

## هيكل العلاقات:

```
Parents (الأهل)
  ├── Children (الأطفال)
  │     ├── Sessions (الجلسات)
  │     ├── child_tasks (مهام الأطفال)
  │     ├── child_badges (شارات الأطفال)
  │     └── task_completions (إكمال المهام)
  │
  ├── Reports (التقارير)
  │
Content (المحتوى)
  ├── Tasks (المهام)
  │     └── child_tasks (مهام الأطفال)
  │
Badges (الشارات)
  └── child_badges (شارات الأطفال)
```

## التحقق من التحديث:

بعد تنفيذ SQL، تحقق من:
1. وجود جميع الجداول الجديدة
2. وجود الحقول المضافة في الجداول الموجودة
3. عمل المفاتيح الخارجية (Foreign Keys)

## الدعم:

إذا واجهت أي مشاكل، تحقق من:
- رسائل الخطأ في phpMyAdmin
- وجود الجداول الأساسية (parents, children)
- توافق إصدار MySQL/MariaDB

