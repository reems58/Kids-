# كيفية إضافة المحتوى التعليمي إلى قاعدة البيانات

## 📋 الخطوات:

### 1️⃣ افتح phpMyAdmin:
```
http://localhost/phpmyadmin
```

### 2️⃣ اختر قاعدة البيانات:
- من القائمة على اليسار، اضغط على: **`kids_learning`**

### 3️⃣ افتح تبويب SQL:
- اضغط على تبويب **SQL** في الأعلى

### 4️⃣ انسخ محتوى الملف:
- افتح الملف: `database/insert_content_complete.sql`
- انسخ **كل** محتوى الملف (Ctrl+A ثم Ctrl+C)

### 5️⃣ الصق في phpMyAdmin:
- الصق المحتوى في مربع النص (Ctrl+V)
- اضغط **تنفيذ** (Go)

---

## ✅ التحقق من نجاح الإضافة:

بعد التنفيذ، نفّذ هذا الاستعلام للتحقق:

```sql
-- التحقق من عدد المحتويات
SELECT COUNT(*) as total FROM content;

-- يجب أن يظهر: 108 (أو أكثر إذا كان هناك محتوى موجود مسبقاً)
```

---

## 🔍 التحقق من محتوى لعمر محدد:

```sql
-- مثال: محتوى للعمر 10
SELECT * FROM content WHERE min_age = 10 AND max_age = 10;

-- يجب أن يظهر 12 محتوى (4 لكل فئة)
```

---

## ⚠️ ملاحظات مهمة:

1. **روابط YouTube**: الروابط حالياً نموذجية (VIDEO_ID_...)
   - يمكنك استبدالها لاحقاً بروابط حقيقية

2. **روابط الألعاب**: الروابط حالياً نموذجية
   - يمكنك استبدالها بروابط ألعاب حقيقية

3. **إذا ظهر خطأ**: تأكد من وجود الأعمدة التالية في جدول `content`:
   - `content_type`
   - `content_url`
   - `content_category`
   
   إذا لم تكن موجودة، نفّذ أولاً:
   ```sql
   -- من ملف: database/fix_add_content_columns.sql
   ```

---

## 📊 الإحصائيات:

- **عدد الفئات**: 3 (عربي، علوم، رياضيات)
- **عدد الأعمار**: 9 (4، 5، 6، 7، 8، 9، 10، 11، 12)
- **المحتوى لكل عمر وفئة**: 4 (2 لعبة + 2 فيديو)
- **الإجمالي**: 108 محتوى

---

**آخر تحديث**: 2025

