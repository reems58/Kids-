# طريقة بسيطة لإضافة المحتوى

## 🎯 الطريقة الأسهل:

### الخطوة 1: إضافة الأعمدة (في phpMyAdmin)

1. افتح: `http://localhost/phpmyadmin`
2. اختر قاعدة البيانات: `kids_learning`
3. اضغط تبويب **SQL**
4. انسخ والصق هذا الكود:

```sql
USE kids_learning;

ALTER TABLE content ADD COLUMN content_type ENUM('لعبة', 'فيديو') DEFAULT 'لعبة';
ALTER TABLE content ADD COLUMN content_url VARCHAR(500) DEFAULT NULL;
ALTER TABLE content ADD COLUMN content_category ENUM('عربي', 'علوم', 'رياضيات') DEFAULT NULL;
```

5. اضغط **تنفيذ**

⚠️ **إذا ظهر خطأ "Duplicate column name"**: 
- يعني الأعمدة موجودة بالفعل ✅
- تجاهل الخطأ وانتقل للخطوة 2

---

### الخطوة 2: إضافة المحتوى

1. في نفس تبويب SQL
2. افتح الملف: `database/add_content_complete.sql`
3. ابدأ من السطر الذي يحتوي على `-- فئة: عربي`
4. انسخ كل المحتوى من هناك
5. الصق في phpMyAdmin
6. اضغط **تنفيذ**

---

## ✅ التحقق:

```sql
SELECT COUNT(*) as total FROM content;
```

يجب أن يظهر: **108 محتوى**

---

## 📝 ملاحظة:

- إذا كان هناك محتوى موجود مسبقاً، قد يظهر عدد أكبر من 108
- هذا طبيعي ✅

---

**آخر تحديث**: 2025

