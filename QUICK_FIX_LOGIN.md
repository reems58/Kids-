# حل سريع: خطأ "غير مسجل دخول" في نافذة إدارة المهام

## 🚨 المشكلة:
عند فتح نافذة "إدارة المهام"، تظهر رسالة:
```
خطأ: غير مسجل دخول. يرجى تسجيل الدخول أولاً
```

## ✅ الحل السريع:

### الخطوة 1: تأكد من تسجيل الدخول
1. افتح المتصفح
2. اذهب إلى: **`http://localhost/kids_learning/html/index.html`**
3. سجل دخول بحساب موجود
4. بعد تسجيل الدخول، سيتم حفظ الجلسة

### الخطوة 2: افتح لوحة تحكم الأهل
بعد تسجيل الدخول، ستنتقل تلقائياً إلى:
```
http://localhost/kids_learning/html/parent_dashboard.html
```

### الخطوة 3: جرب فتح نافذة إدارة المهام
1. اضغط على زر "إدارة المهام" لطفل معين
2. يجب أن تعمل النافذة الآن بدون خطأ

---

## 🔍 اختبار الجلسة:

### اختبار 1: تحقق من الجلسة
افتح في المتصفح:
```
http://localhost/kids_learning/api/get_current_user.php
```

**النتيجة المتوقعة:**
- ✅ إذا كان مسجل دخول: `{"success":true,"user":{...}}`
- ❌ إذا لم يكن مسجل دخول: `{"success":false,"message":"غير مسجل دخول"}`

### اختبار 2: تحقق من localStorage
افتح Console في المتصفح (F12) واكتب:
```javascript
localStorage.getItem('parent_id')
```

**النتيجة المتوقعة:**
- ✅ إذا كان مسجل دخول: رقم (مثل: `1`, `2`, ...)
- ❌ إذا لم يكن مسجل دخول: `null`

---

## ⚠️ ملاحظات مهمة:

1. **لا تفتح الملفات مباشرة:**
   - ❌ خطأ: `file:///C:/xampp/htdocs/kids_learning/html/parent_dashboard.html`
   - ✅ صحيح: `http://localhost/kids_learning/html/parent_dashboard.html`

2. **تأكد من تشغيل XAMPP:**
   - Apache يجب أن يكون مفعّل
   - MySQL يجب أن يكون مفعّل

3. **الجلسات تعمل فقط عبر الخادم:**
   - يجب أن تفتح الصفحة عبر `localhost`
   - لا تعمل الجلسات مع ملفات HTML المفتوحة مباشرة

---

## 🛠️ إذا استمرت المشكلة:

### حل 1: امسح الكاش
1. اضغط `Ctrl + Shift + Delete`
2. امسح الكاش والكوكيز
3. سجل دخول مرة أخرى

### حل 2: تحقق من php.ini
1. افتح: `C:\xampp\php\php.ini`
2. ابحث عن: `session.save_path`
3. تأكد من وجود المجلد

### حل 3: أعد تشغيل Apache
1. في XAMPP Control Panel
2. اضغط Stop على Apache
3. اضغط Start مرة أخرى

---

**آخر تحديث**: 2025

