# دليل تحديث الروابط في قاعدة البيانات

## ⚠️ ملاحظة مهمة:

الروابط المضافة حالياً هي **روابط نموذجية** وليست حقيقية:
- `games.example.com` ❌ (رابط مثال)
- `VIDEO_ID_...` ❌ (معرف مثال)

---

## ✅ الطريقة الأسهل: عبر phpMyAdmin

### الخطوة 1: افتح phpMyAdmin
```
http://localhost/phpmyadmin
```

### الخطوة 2: حدد المحتوى المراد تعديله
1. اختر قاعدة البيانات: `kids_learning`
2. اختر جدول: `content`
3. اضغط **تصفح** (Browse)

### الخطوة 3: عدّل الرابط
1. ابحث عن السجل المراد تعديله
2. اضغط على أيقونة **✏️ تعديل**
3. عدّل حقل `content_url` برابط حقيقي
4. اضغط **تنفيذ**

---

## 🎬 إضافة روابط YouTube حقيقية:

### الخطوة 1: ابحث في YouTube
1. اذهب إلى: https://www.youtube.com
2. ابحث عن: "تعليم الحروف العربية للأطفال" (مثلاً)
3. اختر فيديو مناسب

### الخطوة 2: انسخ الرابط
- انسخ الرابط من شريط العنوان
- مثال: `https://www.youtube.com/watch?v=ABC123XYZ`

### الخطوة 3: الصق في قاعدة البيانات
- استخدم الخطوة 3 أعلاه

---

## 📝 مثال SQL لتحديث رابط:

```sql
USE kids_learning;

-- تحديث رابط فيديو واحد
UPDATE content 
SET content_url = 'https://www.youtube.com/watch?v=YOUR_REAL_VIDEO_ID' 
WHERE content_id = 3;
```

---

## 🔍 البحث عن المحتوى في SQL:

```sql
-- البحث عن فيديوهات العربية
SELECT content_id, content_name_ar, content_url 
FROM content 
WHERE content_category = 'عربي' 
  AND content_type = 'فيديو';
```

---

## 💡 نصيحة:

ابدأ بتحديث بعض الروابط كتجربة قبل تحديثها كلها!

---

**ملاحظة**: تم تحديث الكود بحيث لن يظهر رابط للمحتوى إذا كان الرابط نموذجياً.

**آخر تحديث**: 2025

