-- إدراج محتوى تعليمي شامل للألعاب والفيديوهات
-- 3 فئات (عربي، علوم، رياضيات) × 9 أعمار (4-12) × 10 محتويات (5 ألعاب + 5 فيديوهات) = 270 محتوى

USE kids_learning;

-- ============================================
-- 1. تحديث جدول content (إذا لم تكن الحقول موجودة)
-- ============================================

-- إضافة الحقول الجديدة
ALTER TABLE content 
ADD COLUMN IF NOT EXISTS content_type ENUM('لعبة', 'فيديو') DEFAULT 'لعبة' AFTER content_name_ar;

ALTER TABLE content 
ADD COLUMN IF NOT EXISTS content_url VARCHAR(500) DEFAULT NULL AFTER content_type;

ALTER TABLE content 
ADD COLUMN IF NOT EXISTS content_category ENUM('عربي', 'علوم', 'رياضيات') DEFAULT NULL AFTER category;

-- ============================================
-- 2. إدراج الفئات الأساسية
-- ============================================

INSERT IGNORE INTO content (content_name, content_name_ar, icon, content_category, content_type) VALUES
('Arabic', 'عربي', '📚', 'عربي', 'لعبة'),
('Math', 'رياضيات', '🔢', 'رياضيات', 'لعبة'),
('Science', 'علوم', '🔬', 'علوم', 'لعبة');

-- ============================================
-- 3. إدراج محتوى تعليمي شامل
-- ============================================

-- ملاحظة: سيتم إدراج 270 محتوى موزعة على:
-- 3 فئات × 9 أعمار (4-12) × 10 محتويات لكل عمر = 270 محتوى

-- سأقوم بإنشاء محتوى منظم لكل فئة وعمر

