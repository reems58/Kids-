# روابط قاعدة البيانات

## 🔗 رابط phpMyAdmin (إدارة قاعدة البيانات):

```
http://localhost/phpmyadmin
```

---

## 📊 معلومات قاعدة البيانات:

**اسم قاعدة البيانات:** `kids_learning`

**اسم المستخدم الافتراضي:** `root` (عادة بدون كلمة مرور في XAMPP)

**اسم المستخدم:** `root`
**كلمة المرور:** (فارغة - اتركها كما هي)

---

## 🛠️ كيفية الوصول:

### الطريقة 1: عبر المتصفح
1. افتح المتصفح
2. اذهب إلى: `http://localhost/phpmyadmin`
3. سيتم فتح صفحة phpMyAdmin

### الطريقة 2: التأكد من تشغيل MySQL
- تأكد من أن MySQL يعمل في XAMPP Control Panel
- يجب أن يكون الزر بجانب MySQL أخضر

---

## 📝 الجداول الرئيسية:

بعد فتح phpMyAdmin واختيار قاعدة البيانات `kids_learning`، ستجد الجداول التالية:

1. **parents** - بيانات الأهل
2. **children** - بيانات الأطفال
3. **content** - المحتوى التعليمي
4. **tasks** - المهام
5. **sessions** - الجلسات
6. **badges** - الشارات
7. **reports** - التقارير
8. **child_badges** - شارات الأطفال
9. **child_tasks** - مهام الأطفال
10. **child_task_order** - ترتيب المهام

---

## 🔍 استعلامات سريعة مفيدة:

### التحقق من وجود المحتوى:
```sql
USE kids_learning;
SELECT COUNT(*) as total FROM content;
SELECT * FROM content WHERE min_age = 10 AND max_age = 10;
```

### التحقق من الأطفال:
```sql
SELECT * FROM children;
```

### التحقق من الأهل:
```sql
SELECT * FROM parents;
```

---

## ⚠️ ملاحظات مهمة:

1. **النسخ الاحتياطي**: قم بعمل نسخة احتياطية قبل إجراء تغييرات كبيرة
2. **الحذر**: لا تحذف البيانات إلا إذا كنت متأكداً
3. **التجربة**: استخدم بيئة تجريبية أولاً إذا أمكن

---

**آخر تحديث**: 2025

